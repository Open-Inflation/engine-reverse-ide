# MSRA документация

Эта документация описывает текущее поведение MSRA и, в первую очередь, семантику путей в таблицах.

Если нужна самая важная часть, см. страницу [Пути и identity](msra-paths.md).

Если нужен список текущих ограничений codegen на примере `example.msra`, см. страницу [Ограничения генератора](msra-generator-gaps.md).

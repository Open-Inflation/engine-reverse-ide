# Demo

Generate the demo project:

```powershell
python -m msra_codegen generate .\examples\demo\demo.msra -o .\generated
```

Validate the generated project:

```powershell
python -m msra_codegen validate .\generated
```
